export interface Sales {
  totalSales: number;
  month: string;
}

export interface Orders {
  totalOrders: number;
  month: string;
}

export interface Revenue {
  totalRevenue: number;
  month: string;
}

export interface Users {
  totalUsers: number;
  month: string;
}